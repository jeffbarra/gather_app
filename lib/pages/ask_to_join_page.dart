import 'package:flutter/material.dart';

class AskToJoinPage extends StatelessWidget {
  const AskToJoinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Ask to Join Page')));
  }
}
